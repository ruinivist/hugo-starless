# split on theming

I don't intend to keep the vars for diff theme tokens as part of the core theme itself, that's just bloat. User woul copy and override what's needed.

For my demo site, I'll just override the layout partials.
